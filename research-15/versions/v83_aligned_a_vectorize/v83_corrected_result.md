Corrected v83 targeted the exact INT8/INT4 A staging loop after forensic discovery that the first patch hit the FP16 loop. Local validation passed; Kaggle T4 native correctness passed, but t4_production=failed and terminal_decision=no_go because mixed QKV prefill performance regressed severely (rows 16 e2e speedup 0.284x; rows 128 0.191x). The repair is therefore rejected. Structural reason: a 16x16 int8 tile is 256 bytes, while the fixed 128-thread CTA has only 64 four-byte vectors; int4 vectorization leaves half the threads idle and increases latency/parallelism loss. A further v83 patch would require a new two-stage/tile architecture, not a local fix. Source restored from v83 before-snapshot.
