v83 deep research proposed aligned 4-byte vectorized full-row activation A staging based on NVIDIA Turing coalescing evidence. Local build and notebook validation passed, but Kaggle T4 gate completed with t4_production=failed and no_go; benchmark records show large max_abs_error values, so native correctness failed. The vector load mapping was not numerically equivalent to the WMMA row-major tile. The source was fully reverted to the v51 snapshot; no v83 change kept.
Error localization: qwen mixed rows 16/128 and pure FP16 rows 16/128 reported NaN; pure INT4 and INT8 remained numerically close. Smoke mixed rows 8/32/128 reported large errors. This indicates the full-row vector-load edit interacted with the prefill/shared path beyond the intended INT8 branch, so it is unsafe even though the source compiled.
Forensic root cause: the patch matcher selected the first generic or (int linear...) block at the FP16 A staging loop (around source line 142), not the INT8/INT4 A staging loop (around line 200). It then wrote int8 data into the FP16 branch buffer, explaining NaNs in FP16/mixed rows while pure INT4/INT8 stayed close. The hypothesis itself remains repairable by targeting the exact INT loop only.
