v84 deep research proposed an explicit 8-warp 64x32 output tile. Source inspection showed the current reuse kernel is structurally a 32x64 tile: warp_row=warp/4, warp_channel=warp%4, A shared buffers have 2 tiles, B buffers have 4 tiles, and host dispatch uses the original 64-channel partition. Converting it to 64x32 requires changing warp ownership, all A/B shared allocation and load loop bounds, FP16 and INT paths, precision tile width, host grid, and output mapping as one atomic redesign. A partial/local substitution would risk out-of-bounds shared-memory access or incomplete output coverage. No source change and no Kaggle measurement were made; clean v51 remains active.
